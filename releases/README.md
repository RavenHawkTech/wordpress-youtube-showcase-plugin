# Releases

Place installable RavenHawk YouTube Showcase ZIP packages in this folder when tracking release artifacts directly in the repository.

Preferred long-term distribution is GitHub Releases with immutable release assets referenced by `updates/ravenhawk-youtube-showcase.json`.
